pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                sh 'pwd'  // Print working directory
                sh 'ls -la'  // Show contents
            }
        }
        // Rest of your pipeline
    }
}
